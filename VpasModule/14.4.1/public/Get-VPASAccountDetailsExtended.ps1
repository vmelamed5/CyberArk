<#
.Synopsis
   GET ACCOUNT EXTENDED DETAILS
   CREATED BY: Vadim Melamed, EMAIL: vpasmodule@gmail.com
.DESCRIPTION
   USE THIS FUNCTION TO GET EXTENDED DETAILS OF AN ACCOUNT IN CYBERARK
.LINK
   https://vpasmodule.com/commands/Get-VPASAccountDetailsExtended
.NOTES
   SelfHosted: TRUE
   PrivCloudStandard: TRUE
   SharedServices: TRUE
.PARAMETER token
   HashTable of data containing various pieces of login information (PVWA, LoginToken, HeaderType, etc).
   If -token is not passed, function will use last known hashtable generated by New-VPASToken
.PARAMETER safe
   Safe name that will be used to query for the target account if no AcctID is passed
.PARAMETER username
   Username that will be used to query for the target account if no AcctID is passed
.PARAMETER platform
   PlatformID that will be used to query for the target account if no AcctID is passed
.PARAMETER address
   Address that will be used to query for the target account if no AcctID is passed
.PARAMETER AcctID
   Unique ID that maps to a single account, passing this variable will skip any query functions
.PARAMETER ExactMatch
   Returns accounts that match search query exactly (not a wildcard search)
.PARAMETER HideWarning
   Hide any warning outputs from the console during the API session
.PARAMETER InputParameters
   HashTable of values containing the parameters required to make the API call
.PARAMETER ExportToCSV
   Output results to a CSV file found in AppData\Local\VPASModuleOutputs directory
.PARAMETER CSVDirectory
   Output directory where the exported CSV file will be saved to
   Default value: C:\Users\{CurrentUser}\AppData\Local\VPASModuleOutputs\ExportedCSVs
.EXAMPLE
   $AccountDetailsExtendedJSON = Get-VPASAccountDetailsExtended -safe {SAFE VALUE} -username {USERNAME VALUE}
.EXAMPLE
   $AccountDetailsExtendedJSON = Get-VPASAccountDetailsExtended -AcctID {ACCTID VALUE}
.EXAMPLE
   $InputParameters = @{
        AcctID = 3_12
        ExportToCSV = $true|$false
        CSVDirectory = "C:\Temp"
   }
   $AccountDetailsExtendedJSON = Get-VPASAccountDetailsExtended -InputParameters $InputParameters
.EXAMPLE
   $InputParameters = @{
        safe = "TargetSafeName"
        platform = "TargetPlatformID"
        username = "TargetUsername"
        address = "TargetAddress"
        ExactMatch = $true|$false
        HideWarning = $true|$false
        ExportToCSV = $true|$false
        CSVDirectory = "C:\Temp"
   }
   $AccountDetailsExtendedJSON = Get-VPASAccountDetailsExtended -InputParameters $InputParameters
.OUTPUTS
   If successful:
   {
        "Compliance":  {
                           "IsCompliant":  false,
                           "LastModifiedDate":  1715222718,
                           "LastModifiedBy":  "vadim@vman.com",
                           "ModificationType":  "Change"
                       },
        "Activities":  [
                           ...
                           {
                               "Alert":  false,
                               "Date":  1735008158,
                               "User":  "vpasuser@vman.com",
                               "Action":  "Get File Request",
                               "ActionID":  109,
                               "ClientID":  "PVWA",
                               "MoreInfo":  "",
                               "Reason":  "give me access [From 12/24/2024 2:42:38 AM to 1/23/2025 2:42:38 AM ]"
                           },
                           ...
                       ],
        "TotalDependencies":  0,
        "FailedDependencies":  0,
        "Recordings":  null,
        "Details":  {
                        "LastVerifiedDate":  0,
                        "LastVerifiedBy":  null,
                        "LastUsedBy":  "vadim@vman.cpm",
                        "LastUsedDate":  1715222731,
                        "CreationDate":  1715222718,
                        "Name":  "Operating System-VPASDualControl-vman.com-DomainAdmin01",
                        "CreatedTime":  1715222718,
                        "AccountURL":  null,
                        "ManagedByCPM":  true,
                        "CPMDisabled":  "",
                        "CPMStatus":  null,
                        "CPMErrorDetails":  "",
                        "ImmediateCPMTask":  null,
                        "DeletedBy":  "",
                        "DeletionDate":  0,
                        "LockedBy":  "",
                        "IsFavorite":  false,
                        "IsNew":  false,
                        "SafeName":  "VPASRequestSafe",
                        "IsGroupMember":  false,
                        "DualControlStatus":  "RequestNeeded",
                        "RequiredProperties":  {
                                                   "Address":  "vman.com",
                                                   "Username":  "DomainAdmin01"
                                               },
                        "OptionalProperties":  null,
                        "LimitDomainAccess":  "",
                        "AccessDomainList":  "",
                        "RequestId":  -1,
                        "FutureTimeFrame":  false,
                        "LinkedAccounts":  null
                    },
        "Platform":  {
                         "VerificationPeriod":  7,
                         "ExpirationPeriod":  90,
                         "HeadStartInterval":  5,
                         "PlatformID":  "VPASDualControl",
                         "PlatformName":  "VPASDualControl",
                         "IsActive":  true
                     },
        "AvailableTabs":  [
                              "Activities",
                              "Dependencies"
                          ],
        "ActionsToDisplay":  null
   }
   ---
   $false if failed
#>
function Get-VPASAccountDetailsExtended{
    [OutputType('System.Object','System.Collections.Hashtable',[bool])]
    [CmdletBinding(DefaultParameterSetName='Set1')]
    Param(

        [Parameter(Mandatory=$false,ParameterSetName='Set1',ValueFromPipelineByPropertyName=$true)]
        [String]$safe,

        [Parameter(Mandatory=$false,ParameterSetName='Set1',ValueFromPipelineByPropertyName=$true)]
        [String]$platform,

        [Parameter(Mandatory=$false,ParameterSetName='Set1',ValueFromPipelineByPropertyName=$true)]
        [String]$username,

        [Parameter(Mandatory=$false,ParameterSetName='Set1',ValueFromPipelineByPropertyName=$true)]
        [String]$address,

        [Parameter(Mandatory=$true,ParameterSetName='Set2',ValueFromPipelineByPropertyName=$true,HelpMessage="Unique AccountID that references the target account (for example: 3_12)")]
        [String]$AcctID,

        [Parameter(Mandatory=$false,ParameterSetName='Set1',ValueFromPipelineByPropertyName=$true)]
        [Switch]$ExactMatch,

        [Parameter(Mandatory=$false,ParameterSetName='Set1',ValueFromPipelineByPropertyName=$true)]
        [Switch]$HideWarning,

        [Parameter(Mandatory=$true,ParameterSetName='InputParameters',ValueFromPipelineByPropertyName=$true,HelpMessage="Hashtable of parameters required to make API call, refer to get-help -examples for valid inputs")]
        [hashtable]$InputParameters,

        [Parameter(Mandatory=$false,ParameterSetName='Set1',ValueFromPipelineByPropertyName=$true)]
        [Parameter(Mandatory=$false,ParameterSetName='Set2',ValueFromPipelineByPropertyName=$true)]
        [Switch]$ExportToCSV,

        [Parameter(Mandatory=$false,ParameterSetName='Set1',ValueFromPipelineByPropertyName=$true)]
        [Parameter(Mandatory=$false,ParameterSetName='Set2',ValueFromPipelineByPropertyName=$true)]
        [String]$CSVDirectory,

        [Parameter(Mandatory=$false,ValueFromPipelineByPropertyName=$true)]
        [hashtable]$token
    )

    Begin{
        $tokenval,$sessionval,$PVWA,$Header,$ISPSS,$IdentityURL,$EnableTextRecorder,$AuditTimeStamp,$NoSSL,$VaultVersion,$HideWarnings,$AuthenticatedAs,$SubDomain,$EnableTroubleshooting = Get-VPASSession -token $token
        $CommandName = $MyInvocation.MyCommand.Name
        $log = Write-VPASTextRecorder -inputval $CommandName -token $token -LogType COMMAND
    }
    Process{
        try{
            if($PSCmdlet.ParameterSetName -eq "InputParameters"){
                $KeyHash = @{
                    set1 = @{
                        AcceptableKeys = @("safe","platform","username","address","ExactMatch","HideWarning","SavedFilter","ExportToCSV","CSVDirectory")
                        MandatoryKeys = @()
                    }
                    set2 = @{
                        AcceptableKeys = @("AcctID","ExportToCSV","CSVDirectory")
                        MandatoryKeys = @("AcctID")
                    }
                }
                $CheckSet = Test-VPASHashtableKeysHelper -InputHash $InputParameters -KeyHash $KeyHash

                if(!$CheckSet){
                    $log = Write-VPASTextRecorder -inputval "FAILED TO FIND TARGET PARAMETER SET" -token $token -LogType MISC
                    Write-Verbose "FAILED TO FIND TARGET PARAMETER SET"
                    Write-VPASOutput -str "FAILED TO FIND TARGET PARAMETER SET...VIEW EXAMPLES BELOW:" -type E
                    $examples = Write-VPASExampleHelper -CommandName $CommandName
                    return $false
                }
                else{
                    foreach($key in $InputParameters.Keys){
                        Set-Variable -Name $key -Value $InputParameters.$key
                    }
                }
            }
        }catch{
            $log = Write-VPASTextRecorder -inputval $_ -token $token -LogType ERROR
            $log = Write-VPASTextRecorder -inputval "REST API COMMAND RETURNED: FALSE" -token $token -LogType MISC
            Write-Verbose "FAILED TO GET ACCOUNT DETAILS"
            Write-VPASOutput -str $_ -type E
            return $false
        }

        try{
            $ExtendedOutput = @()
            if([String]::IsNullOrEmpty($AcctID)){
                Write-Verbose "NO ACCTID SUPPLIED"
                Write-Verbose "BUILDING SEARCH QUERY"
                $searchQuery = "$safe $platform $username $address"
                Write-verbose "MAKING API CALL TO CYBERARK"

                if($NoSSL){
                    Write-Verbose "NO SSL ENABLED, USING HTTP INSTEAD OF HTTPS"
                    $uri = "http://$PVWA/PasswordVault/api/Accounts?limit=1000&search=$searchQuery"
                }
                else{
                    Write-Verbose "SSL ENABLED BY DEFAULT, USING HTTPS"
                    $uri = "https://$PVWA/PasswordVault/api/Accounts?limit=1000&search=$searchQuery"
                }
                $log = Write-VPASTextRecorder -inputval $uri -token $token -LogType URI
                $log = Write-VPASTextRecorder -inputval "GET" -token $token -LogType METHOD

                $output = @{
                    count = 0
                    value = ""
                }

                if($sessionval){
                    $response = Invoke-RestMethod -Headers @{"Authorization"=$Header} -Uri $uri -Method GET -ContentType "application/json" -WebSession $sessionval
                }
                else{
                    $response = Invoke-RestMethod -Headers @{"Authorization"=$Header} -Uri $uri -Method GET -ContentType "application/json"
                }
                $log = Write-VPASTextRecorder -inputval $response -token $token -LogType RETURNARRAY

                $output.count = $response.count
                $output.value = $response.value
                $nextlink = $response.nextLink
                while(![String]::IsNullOrEmpty($nextlink)){
                    if($NoSSL){
                        Write-Verbose "NO SSL ENABLED, USING HTTP INSTEAD OF HTTPS"
                        $uri = "http://$PVWA/PasswordVault/$nextlink"
                    }
                    else{
                        Write-Verbose "SSL ENABLED BY DEFAULT, USING HTTPS"
                        $uri = "https://$PVWA/PasswordVault/$nextlink"
                    }
                    $log = Write-VPASTextRecorder -inputval $uri -token $token -LogType URI
                    $log = Write-VPASTextRecorder -inputval "GET" -token $token -LogType METHOD

                    if($sessionval){
                        $response = Invoke-RestMethod -Headers @{"Authorization"=$Header} -Uri $uri -Method GET -ContentType "application/json" -WebSession $sessionval
                    }
                    else{
                        $response = Invoke-RestMethod -Headers @{"Authorization"=$Header} -Uri $uri -Method GET -ContentType "application/json"
                    }
                    $log = Write-VPASTextRecorder -inputval $response -token $token -LogType RETURN

                    $output.count += $response.count
                    $output.value += $response.value
                    $nextlink = $response.nextLink
                }

                if($ExactMatch){
                    Write-Verbose "PARSING DATA FROM CYBERARK"
                    $newoutput = @{}
                    $arr = @()
                    $newcount = 0
                    foreach($rec in $output.value){
                        $curMatch = $true
                        $recSafeName = $rec.safeName
                        $recPlatform = $rec.platformId
                        $recAddress = $rec.address
                        $recUsername = $rec.userName

                        if(![String]::IsNullOrEmpty($safe)){ if($safe -ne $recSafeName){ $curMatch = $false }}
                        if(![String]::IsNullOrEmpty($platform)){ if($platform -ne $recPlatform){ $curMatch = $false }}
                        if(![String]::IsNullOrEmpty($address)){ if($address -ne $recAddress){ $curMatch = $false }}
                        if(![String]::IsNullOrEmpty($username)){ if($username -ne $recUsername){ $curMatch = $false }}

                        if($curMatch){
                            $newcount += 1
                            $arr += $rec
                        }
                    }
                    $newoutput = @{
                        count = $newcount
                        value = $arr
                    }
                    $result = $newoutput
                }
                else{
                    $result = $output
                    Write-Verbose "PARSING DATA FROM CYBERARK"
                }

                $counter = $result.count
                if($counter -gt 1){
                    if(!$HideWarning){
                        Write-VPASOutput -str "MULTIPLE ENTRIES FOUND, ADD MORE SEARCH FIELDS TO NARROW DOWN RESULTS" -type M
                    }
                    Write-Verbose "MULTIPLE RECORDS WERE RETURNED, ADD MORE SEARCH FIELDS TO NARROW DOWN RESULTS"
                }
                elseif($counter -eq 0){
                    Write-Verbose "NO ACCOUNTS FOUND WITH SPECIFIED PARAMETERS"
                    if(!$HideWarning){
                        Write-VPASOutput -str "NO ACCOUNTS FOUND" -type M
                    }
                    $log = Write-VPASTextRecorder -inputval "NO ACCOUNTS FOUND" -token $token -LogType MISC
                    $log = Write-VPASTextRecorder -inputval "REST API COMMAND RETURNED: FALSE" -token $token -LogType MISC
                    return $false
                }
                #-------------------------------------
                Write-Verbose "FETCHING EXTENDED DETAILS FOR EACH RETURNED ACCOUNT"
                foreach($rec in $result.value){
                    $TargetAcctID = $rec.id
                    write-verbose "ANALYZING ACCOUNTID: $TargetAcctID"

                    if($NoSSL){
                        Write-Verbose "NO SSL ENABLED, USING HTTP INSTEAD OF HTTPS"
                        $uri = "http://$PVWA/PasswordVault/api/ExtendedAccounts/$TargetAcctID/overview"
                    }
                    else{
                        Write-Verbose "SSL ENABLED BY DEFAULT, USING HTTPS"
                        $uri = "https://$PVWA/PasswordVault/api/ExtendedAccounts/$TargetAcctID/overview"
                    }
                    $log = Write-VPASTextRecorder -inputval $uri -token $token -LogType URI
                    $log = Write-VPASTextRecorder -inputval "GET" -token $token -LogType METHOD

                    if($sessionval){
                        $extdetails = Invoke-RestMethod -Headers @{"Authorization"=$Header} -Uri $uri -Method GET -ContentType "application/json" -WebSession $sessionval
                    }
                    else{
                        $extdetails = Invoke-RestMethod -Headers @{"Authorization"=$Header} -Uri $uri -Method GET -ContentType "application/json"
                    }
                    $extdetails | Add-Member -NotePropertyName AccountID -NotePropertyValue $TargetAcctID
                    $log = Write-VPASTextRecorder -inputval $response -token $token -LogType RETURN
                    $ExtendedOutput += $extdetails
                }
                if($ExportToCSV){
                    $runexport = Write-VPASExportCSV -Data $ExtendedOutput -CommandName $CommandName -CSVDirectory $CSVDirectory
                }
                return $ExtendedOutput
            }
            else{
                Write-Verbose "ACCTID SUPPLIED, SKIPPING SEARCH QUERY"
                if($NoSSL){
                    Write-Verbose "NO SSL ENABLED, USING HTTP INSTEAD OF HTTPS"
                    $uri = "http://$PVWA/PasswordVault/api/ExtendedAccounts/$AcctID/overview"
                }
                else{
                    Write-Verbose "SSL ENABLED BY DEFAULT, USING HTTPS"
                    $uri = "https://$PVWA/PasswordVault/api/ExtendedAccounts/$AcctID/overview"
                }
                $log = Write-VPASTextRecorder -inputval $uri -token $token -LogType URI
                $log = Write-VPASTextRecorder -inputval "GET" -token $token -LogType METHOD

                if($sessionval){
                    $response = Invoke-RestMethod -Headers @{"Authorization"=$Header} -Uri $uri -Method GET -ContentType "application/json" -WebSession $sessionval
                }
                else{
                    $response = Invoke-RestMethod -Headers @{"Authorization"=$Header} -Uri $uri -Method GET -ContentType "application/json"
                }
                $response | Add-Member -NotePropertyName AccountID -NotePropertyValue $AcctID
                $ExtendedOutput += $response
                $log = Write-VPASTextRecorder -inputval $ExtendedOutput -token $token -LogType RETURN
                if($ExportToCSV){
                    $runexport = Write-VPASExportCSV -Data $ExtendedOutput -CommandName $CommandName -CSVDirectory $CSVDirectory
                }
                return $ExtendedOutput
            }
        }catch{
            $log = Write-VPASTextRecorder -inputval $_ -token $token -LogType ERROR
            $log = Write-VPASTextRecorder -inputval "REST API COMMAND RETURNED: FALSE" -token $token -LogType MISC
            Write-Verbose "COULD NOT GET ACCOUNT EXTENDED DETAILS"
            Write-VPASOutput -str $_ -type E
            return $false
        }
    }
    End{
        $log = Write-VPASTextRecorder -inputval $CommandName -token $token -LogType DIVIDER
    }
}
